<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nguegangbeth.openprices">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logos"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.Interface_connexion_Activity"></activity>
        <activity android:name=".views.QuestionActivity" />
        <activity android:name=".views.Creer_compte_userActivity" />
        <activity android:name=".views.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.User_no_connectActivity" />
        <activity android:name=".views.ChoixConnexionActivity" />
        <activity android:name=".views.DescriptionActivity" />
    </application>

</manifest>